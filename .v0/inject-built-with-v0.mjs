// Compatibility shim for the Vercel/v0 project build command.
// The deployed project invokes this script before `next build`.
// Pour does not need any build-time mutation here, so this intentionally exits cleanly.
